# Number of Consul servers to expect for forming a quorum and electing a leader. Use 1 for dev/single-server. Use 3, 5, etc. for HA/production clusters. Must match the number of server nodes.
BOOTSTRAP_EXPECT=

# Unique identifier for this Consul node. Shows up in the Consul UI and logs. Use something meaningful (e.g., consul-server-1).
CONSUL_NODE_NAME=node1

# Logical datacenter name for this Consul cluster. Useful for separating regions, environments, or test clusters (e.g., dc1, prod-eu, staging).
CONSUL_DATACENTER=dc1

CONSUL_BIND_ADDR=

CONSUL_CONTENT_PATH=

CONSUL_UI_IS_ENABLED= #true/false